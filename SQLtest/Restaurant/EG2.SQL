SELECT 
	MENU_ITEM_NAME,
	PRICE,
	CASE PRICE
		WHEN (SELECT MAX(PRICE) FROM MENU_ITEM) THEN 'MOST'
		WHEN (SELECT MIN(PRICE) FROM MENU_ITEM) THEN 'LEAST'
	ELSE
		'IN BETWEEN'
	END AS EXTREMITY
FROM 
MENU_ITEM

